type Props = {};

const Header = (props: Props) => {
  return (
    <h1 className="text-center text-3xl font-bold py-2 border-t border-t-primary border-t-4">
      CookBook
    </h1>
  );
};

export default Header;
